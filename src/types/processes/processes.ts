export enum ProcessStatus {
    SUCCESS = 'SUCCESS',
    ERROR = 'ERROR',
    PENDING = 'PENDING',
}

export enum FilterProcessStatus {
    SUCCESS = 'Filtered embeddings',
    ERROR = 'No similar images found',
}